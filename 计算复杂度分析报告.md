# Net模型计算复杂度（FLOPs）分析报告

## 一、总体概况

- **总FLOPs**: 25.95 GFLOPs
- **输入尺寸**: (145, 145, 200)
- **序列长度**: 21,025 (145 × 145)

## 二、各模块FLOPs详细分析

### 1. 预处理层 (LayerNorm)
- **FLOPs**: 8.41 MFLOPs (0.03%)
- **说明**: 计算量极小，可忽略

### 2. 注意力模块 (MultiAttention)
- **总FLOPs**: 18.91 MFLOPs (0.07%)
  - 通道注意力: 8.42 MFLOPs
  - 空间注意力: 10.49 MFLOPs
- **说明**: 计算量很小，可以保持

### 3. 三分支特征提取

#### 分支1: DepthwiseSeparableASPP
- **FLOPs**: 113.54 MFLOPs (0.44%)
- **结构**: 3个膨胀卷积 (dilation=[9, 11, 13])
- **计算**: 每个膨胀卷积 = H × W × bands × 3 × 3

#### 分支2: MultiScaleAsymmetricDepthConv ⚠️
- **FLOPs**: 428.91 MFLOPs (1.65%)
- **结构**: 3个非对称卷积对 (kernel=[15, 17, 19])
- **计算**: 
  - 1×15卷积: H × W × bands × 15
  - 15×1卷积: H × W × bands × 15
  - 每个卷积对 = 2 × H × W × bands × kernel_size
- **说明**: 非对称卷积的计算量相对较大

#### 分支3: DepthwiseSeparableSquareConv
- **FLOPs**: 349.02 MFLOPs (1.35%)
- **结构**: 3个方形卷积 (kernel=[3, 5, 7])
- **计算**: 每个方形卷积 = H × W × bands × kernel_size²

**三个分支总FLOPs**: 891.48 MFLOPs (3.44%)

### 4. 融合层 ⚠️ **最大瓶颈1**
- **FLOPs**: 12,928.52 MFLOPs (49.83%)
- **结构**:
  - Conv2d(2400→256): 12,917.76 MFLOPs
  - BatchNorm2d: 10.76 MFLOPs
- **计算公式**: 
  - Conv2d FLOPs = H × W × 2400 × 256 = 145 × 145 × 2400 × 256
- **问题**: 
  - 输入通道数过大 (2400)，导致计算量巨大
  - 占总计算量的近50%

### 5. Mamba层 ⚠️ **最大瓶颈2**
- **总FLOPs**: 11,206.16 MFLOPs (43.19%)
  - Mamba正向: 5,597.70 MFLOPs
  - Mamba反向: 5,597.70 MFLOPs
  - 融合归一化: 10.76 MFLOPs
- **计算公式**:
  - 投影层: seq_len × d_model × d_model × expand × 2
  - SSM计算: seq_len × d_model × d_state
  - 总计算量 ≈ seq_len × d_model² × 4
- **问题**: 
  - 双向Mamba导致计算量翻倍
  - 序列长度大 (21,025) 导致计算量巨大

### 6. 分类器
- **FLOPs**: 892.13 MFLOPs (3.44%)
- **结构**: 
  - Linear(256→128): seq_len × 256 × 128
  - Linear(128→64): seq_len × 128 × 64
  - Linear(64→17): seq_len × 64 × 17
- **说明**: 计算量合理，但可以进一步精简

## 三、各模块FLOPs占比排序

| 模块 | FLOPs (MFLOPs) | 占比 | 优先级 |
|------|----------------|------|--------|
| **融合层** | 12,928.52 | **49.83%** | 🔴 最高 |
| **Mamba层** | 11,206.16 | **43.19%** | 🔴 最高 |
| 分类器 | 892.13 | 3.44% | 🟡 中等 |
| 分支2 (非对称卷积) | 428.91 | 1.65% | 🟡 中等 |
| 分支3 (方形卷积) | 349.02 | 1.35% | 🟢 较低 |
| 分支1 (ASPP) | 113.54 | 0.44% | 🟢 较低 |
| 注意力模块 | 18.91 | 0.07% | 🟢 较低 |
| 预处理层 | 8.41 | 0.03% | 🟢 较低 |

## 四、计算复杂度精简建议

### 🔴 **高优先级 - 必须优化**

#### 1. 优化融合层（预计减少 50-75% 计算量）

**方案A: 减少融合层输入通道数**
```python
# 当前: 2400通道
# 优化后: 1200通道（减少50%）
# FLOPs减少: 12,928 → 6,464 MFLOPs (减少50%)

# 实现方式：
# - 减少ASPP膨胀率数量: 3个 → 2个
# - 减少非对称卷积核数量: 3个 → 2个
# - 减少方形卷积核数量: 3个 → 2个
```

**方案B: 在各分支内部先压缩通道**
```python
# 在每个分支输出前添加1x1卷积压缩到100通道
# 优化后融合层输入: 100×3 + 100×3×2 + 100×3 = 600通道
# FLOPs减少: 12,928 → 3,232 MFLOPs (减少75%)

# 实现方式：
# 在每个分支后添加: Conv2d(bands, bands//2, 1)
```

**方案C: 使用分组卷积或深度可分离卷积**
```python
# 将融合层的1x1卷积改为分组卷积
# 使用groups=8或16，可以显著减少计算量
# FLOPs减少: 约30-50%
```

#### 2. 优化Mamba层（预计减少 30-50% 计算量）

**方案A: 减小d_model**
```python
# 当前: d_model=256
# 优化: d_model=128或192
# FLOPs减少: 11,206 → 约5,600 (减少50%) 或 约7,900 (减少30%)

# Mamba的FLOPs与d_model²成正比
```

**方案B: 使用单方向Mamba**
```python
# 移除反向Mamba，只保留正向
# FLOPs减少: 11,206 → 5,603 MFLOPs (减少50%)

# 但可能影响性能，需要实验验证
```

**方案C: 序列下采样**
```python
# 在Mamba处理前对空间维度进行下采样
# 例如: (145, 145) → (73, 73) 或 (97, 97)
# 序列长度: 21,025 → 5,329 或 9,409
# FLOPs减少: 约50-75%

# 实现方式：
# 在融合层后添加: AvgPool2d(kernel_size=2, stride=2)
# 在分类器前添加: Upsample或转置卷积恢复尺寸
```

**方案D: 使用更轻量的序列模型**
```python
# 考虑使用LSTM或轻量级Transformer
# 但需要重新评估性能
```

### 🟡 **中优先级 - 建议优化**

#### 3. 优化分支2（非对称卷积）
```python
# 当前: 3个卷积对 [15, 17, 19]
# 优化: 2个卷积对 [15, 19] 或减小核大小 [11, 13, 15]
# FLOPs减少: 428.91 → 约286 (减少33%) 或 约350 (减少18%)
```

#### 4. 精简分类器
```python
# 当前: 256 → 128 → 64 → 17
# 优化: 256 → 64 → 17 (移除中间层)
# FLOPs减少: 892.13 → 约400 (减少55%)
```

#### 5. 优化分支3（方形卷积）
```python
# 当前: 3个卷积核 [3, 5, 7]
# 优化: 2个卷积核 [3, 7]
# FLOPs减少: 349.02 → 约233 (减少33%)
```

### 🟢 **低优先级 - 可选优化**

#### 6. 减少分支1（ASPP）膨胀率数量
```python
# 当前: 3个膨胀率 [9, 11, 13]
# 优化: 2个膨胀率 [9, 13]
# FLOPs减少: 113.54 → 75.69 (减少33%)
```

## 五、优化效果预估

### 方案1: 激进优化（预计减少 70-80% FLOPs）
- 融合层通道压缩到600 + d_model=128 + 单方向Mamba + 序列下采样
- **优化前**: 25.95 GFLOPs
- **优化后**: 约 5-8 GFLOPs
- **加速比**: 约 3-5倍

### 方案2: 平衡优化（预计减少 50-60% FLOPs）⭐ **推荐**
- 减少分支数量 + d_model=192 + 保持双向Mamba
- **优化前**: 25.95 GFLOPs
- **优化后**: 约 10-13 GFLOPs
- **加速比**: 约 2-2.5倍

### 方案3: 保守优化（预计减少 30-40% FLOPs）
- 仅减少分支数量 + 精简分类器
- **优化前**: 25.95 GFLOPs
- **优化后**: 约 15-18 GFLOPs
- **加速比**: 约 1.4-1.7倍

## 六、关键发现

### 计算复杂度分布
1. **融合层和Mamba层占总计算量的93%**
   - 融合层: 49.83%
   - Mamba层: 43.19%
   - 其他模块: 仅7%

2. **三个分支计算量相对较小** (3.44%)
   - 虽然分支数量多，但由于使用深度可分离卷积，计算量不大
   - 主要问题在于输出通道数累加导致融合层计算量大

3. **序列长度影响Mamba计算量**
   - 序列长度 = 145 × 145 = 21,025
   - Mamba的FLOPs与序列长度线性相关
   - 下采样可以显著减少计算量

### 与参数量分析的对比

| 指标 | 融合层 | Mamba层 | 其他 |
|------|--------|---------|------|
| **参数量占比** | 37.57% | 56.84% | 5.59% |
| **FLOPs占比** | 49.83% | 43.19% | 6.98% |

- **融合层**: FLOPs占比(49.83%) > 参数量占比(37.57%)
  - 说明融合层的计算密度更高，优化优先级更高
  
- **Mamba层**: 参数量占比(56.84%) > FLOPs占比(43.19%)
  - 说明Mamba层参数多但计算相对高效

## 七、综合优化建议

### 优先级排序
1. **融合层优化** - 计算量和参数量都很大
2. **Mamba层优化** - 参数量最大，计算量也很大
3. **分支数量优化** - 间接影响融合层
4. **分类器优化** - 计算量和参数量都较小，但容易优化

### 推荐优化方案
采用**平衡优化方案**，在保持模型性能的同时，显著减少计算复杂度：
- 减少各分支的卷积核数量（3个→2个）
- 将d_model从256减小到192
- 保持双向Mamba以维持性能
- 精简分类器（移除中间层）

预计可以减少50-60%的计算量，同时保持较好的模型性能。

