# Net模型参数量和内存占用分析报告

## 一、总体概况

- **总参数量**: 1,637,571 参数 (1.64M)
- **参数内存**: 6.25 MB
- **激活值内存**: 621.99 MB ⚠️ **非常大**
- **总内存占用**: 628.23 MB

## 二、各模块详细分析

### 1. 预处理层 (LayerNorm)
- **参数量**: 400 参数 (0.02%)
- **说明**: 参数量极小，无需优化

### 2. 注意力模块 (MultiAttention)
- **总参数量**: 4,898 参数 (0.30%)
  - 通道注意力: 4,800 参数
  - 空间注意力: 98 参数
- **说明**: 参数量占比很小，可以保持

### 3. 三分支特征提取

#### 分支1: DepthwiseSeparableASPP
- **参数量**: 5,400 参数 (0.33%)
- **结构**: 3个膨胀卷积 (dilation=[9, 11, 13])
- **输出通道数**: 200 × 3 = 600

#### 分支2: MultiScaleAsymmetricDepthConv
- **参数量**: 21,600 参数 (1.32%)
- **结构**: 3个非对称卷积对 (kernel=[15, 17, 19])
- **输出通道数**: 200 × 3 × 2 = 1,200

#### 分支3: DepthwiseSeparableSquareConv
- **参数量**: 16,600 参数 (1.01%)
- **结构**: 3个方形卷积 (kernel=[3, 5, 7])
- **输出通道数**: 200 × 3 = 600

**三个分支总参数量**: 43,600 参数 (2.66%)
**三个分支总输出通道数**: 600 + 1,200 + 600 = **2,400** ⚠️

### 4. 融合层 ⚠️ **主要瓶颈1**
- **参数量**: 615,168 参数 (37.57%)
- **结构**: 
  - Conv2d(2400 → 256): 614,656 参数
  - BatchNorm2d(256): 512 参数
- **问题**: 
  - 输入通道数过大 (2400)，导致融合层参数量巨大
  - 计算公式: 2400 × 256 = 614,400 (加上bias)

### 5. Mamba层 ⚠️ **主要瓶颈2**
- **总参数量**: 930,864 参数 (56.84%)
  - Mamba正向: 465,176 参数
  - Mamba反向: 465,176 参数
  - 融合归一化: 512 参数
- **问题**: 
  - 参数量占比超过一半
  - 双向Mamba导致参数量翻倍

### 6. 分类器
- **参数量**: 42,641 参数 (2.60%)
- **结构**: 
  - Linear(256 → 128): 32,896 参数
  - Linear(128 → 64): 8,256 参数
  - Linear(64 → num_classes): 1,105 参数
- **说明**: 参数量合理，但可以进一步精简

## 三、内存占用分析

### 参数内存
- **大小**: 6.25 MB
- **说明**: 相对较小，主要受参数量影响

### 激活值内存 ⚠️ **严重问题**
- **大小**: 621.99 MB
- **说明**: 
  - 激活值内存是参数内存的 **99.5倍**！
  - 主要来源：
    1. 融合层输入特征图: 2400通道 × H × W
    2. Mamba序列处理: (H×W) × d_model
    3. 中间特征图缓存

## 四、精简建议（按优先级排序）

### 🔴 **高优先级 - 必须优化**

#### 1. 减少融合层输入通道数（预计减少 50-70% 参数量）

**方案A: 减少分支数量**
```python
# 当前配置
dilations=[9, 11, 13]  # 3个 → 建议改为2个: [9, 13]
kernel_sizes=[15, 17, 19]  # 3个 → 建议改为2个: [15, 19]
kernel_sizes=[3, 5, 7]  # 3个 → 建议改为2个: [3, 7]

# 优化后融合层输入通道数: 200×2 + 200×2×2 + 200×2 = 1,200 (减少50%)
# 融合层参数量: 1,200 × 256 = 307,200 (减少约50%)
```

**方案B: 在各分支内部先进行通道压缩**
```python
# 在每个分支输出前添加1x1卷积压缩通道
# 例如：将每个分支的输出压缩到 bands//2 = 100
# 优化后融合层输入通道数: 100×3 + 100×3×2 + 100×3 = 600 (减少75%)
# 融合层参数量: 600 × 256 = 153,600 (减少约75%)
```

**方案C: 减少d_model**
```python
# 将d_model从256减少到128或192
# 融合层参数量: 2400 × 128 = 307,200 (减少50%)
# 但会影响Mamba层和分类器
```

#### 2. 优化Mamba层（预计减少 30-50% 参数量）

**方案A: 减小d_model**
```python
# 将d_model从256减少到128或192
# Mamba参数量会相应减少（Mamba参数量与d_model的平方相关）
```

**方案B: 使用单方向Mamba**
```python
# 移除反向Mamba，只保留正向
# 参数量减少50%: 930,864 → 465,432
# 但可能影响性能
```

**方案C: 使用更轻量的序列模型**
```python
# 考虑使用Transformer的轻量版本或LSTM
# 但需要重新评估性能
```

### 🟡 **中优先级 - 建议优化**

#### 3. 精简分类器
```python
# 当前: 256 → 128 → 64 → num_classes
# 优化: 256 → 64 → num_classes (减少中间层)
# 参数量: 42,641 → 约20,000 (减少约53%)
```

#### 4. 减少非对称卷积核大小
```python
# 当前: [15, 17, 19]
# 优化: [11, 13, 15] 或 [13, 15]
# 参数量: 21,600 → 约14,000 (减少约35%)
```

### 🟢 **低优先级 - 可选优化**

#### 5. 减少ASPP膨胀率数量
```python
# 当前: [9, 11, 13] (3个)
# 优化: [9, 13] (2个)
# 参数量: 5,400 → 3,600 (减少33%)
```

#### 6. 减少方形卷积核数量
```python
# 当前: [3, 5, 7] (3个)
# 优化: [3, 7] (2个)
# 参数量: 16,600 → 约11,000 (减少33%)
```

## 五、优化效果预估

### 方案1: 激进优化（预计减少70%参数量）
- 减少分支数量 + 减小d_model到128 + 单方向Mamba
- **优化前**: 1.64M 参数
- **优化后**: 约 0.5M 参数
- **内存减少**: 约 400MB

### 方案2: 平衡优化（预计减少50%参数量）
- 减少分支数量 + 减小d_model到192 + 保持双向Mamba
- **优化前**: 1.64M 参数
- **优化后**: 约 0.8M 参数
- **内存减少**: 约 300MB

### 方案3: 保守优化（预计减少30%参数量）
- 仅减少分支数量 + 精简分类器
- **优化前**: 1.64M 参数
- **优化后**: 约 1.1M 参数
- **内存减少**: 约 200MB

## 六、总结

### 主要问题
1. **融合层参数量过大** (37.57%) - 由输入通道数2400导致
2. **Mamba层参数量过大** (56.84%) - 双向Mamba + 大d_model
3. **激活值内存占用巨大** (621.99 MB) - 主要由大特征图导致

### 优化重点
1. **优先优化融合层** - 通过减少分支输出通道数
2. **其次优化Mamba层** - 通过减小d_model或使用单方向
3. **最后优化分类器** - 减少中间层

### 建议
建议采用**方案2（平衡优化）**，在保持模型性能的同时，显著减少参数量和内存占用。

---

## 七、计算复杂度（FLOPs）分析

### 总体概况
- **总FLOPs**: 25.95 GFLOPs
- **主要瓶颈**: 融合层(49.83%) + Mamba层(43.19%) = 93.02%

### 各模块FLOPs占比

| 模块 | FLOPs (MFLOPs) | 占比 |
|------|----------------|------|
| **融合层** | 12,928.52 | **49.83%** |
| **Mamba层** | 11,206.16 | **43.19%** |
| 分类器 | 892.13 | 3.44% |
| 分支2 (非对称卷积) | 428.91 | 1.65% |
| 分支3 (方形卷积) | 349.02 | 1.35% |
| 分支1 (ASPP) | 113.54 | 0.44% |
| 注意力模块 | 18.91 | 0.07% |
| 预处理层 | 8.41 | 0.03% |

### 计算复杂度精简建议

#### 1. 优化融合层（预计减少50-75%计算量）
- **方案A**: 减少分支数量，将融合层输入从2400降到1200
- **方案B**: 在各分支内部先压缩通道到100
- **方案C**: 使用分组卷积替代1x1卷积

#### 2. 优化Mamba层（预计减少30-50%计算量）
- **方案A**: 减小d_model从256到128或192
- **方案B**: 使用单方向Mamba（减少50%）
- **方案C**: 序列下采样（减少50-75%）

#### 3. 优化分支和分类器
- 减少非对称卷积核数量
- 精简分类器（移除中间层）

### 优化效果预估

- **激进优化**: 25.95 → 5-8 GFLOPs (减少70-80%，加速3-5倍)
- **平衡优化**: 25.95 → 10-13 GFLOPs (减少50-60%，加速2-2.5倍) ⭐ **推荐**
- **保守优化**: 25.95 → 15-18 GFLOPs (减少30-40%，加速1.4-1.7倍)

**详细分析请参考**: `计算复杂度分析报告.md`

